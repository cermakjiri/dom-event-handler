import { expect } from "chai";
import DomEventHandler from "../index";

describe("DomEventHandler() class", () => {
    describe("attach()", () => {
        // const handler = new DomEventHandler();

        it("should attach a handler for an DOM event", done => {
            // expect(handler.attach(window, ))

            done();
        });
    });
});
